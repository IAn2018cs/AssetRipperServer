# Application Settings
API_PORT=8000
LOG_LEVEL=INFO

# File Cleanup Settings
FILE_RETENTION_DAYS=30

# AssetRipper Settings (for local development)
# Use external AssetRipper instance (if set, will not start AssetRipper process)
# ASSETRIPPER_HOST=http://localhost:8765

# Path to AssetRipper binary (only needed if ASSETRIPPER_HOST is not set)
# ASSETRIPPER_BINARY_PATH=/path/to/AssetRipper.GUI.Free
